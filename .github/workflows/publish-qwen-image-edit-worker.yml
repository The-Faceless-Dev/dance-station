name: Publish Qwen Image Edit 2511 Worker

on:
  push:
    branches:
      - wan-animate-int8-convrot-experiment
    paths:
      - ".github/workflows/publish-qwen-image-edit-worker.yml"
      - "containers/qwen-image-edit-worker/**"
      - "src/autotransition/qwen_image_edit/**"
      - "pyproject.toml"
  workflow_dispatch:
    inputs:
      tag:
        description: Public Qwen Image Edit worker tag
        required: true
        default: qwen-image-edit-2511-q8-20260914-r1

permissions:
  contents: read
  packages: write

concurrency:
  group: qwen-image-edit-worker-publish
  cancel-in-progress: true

env:
  IMAGE: ghcr.io/the-faceless-dev/faceless-qwen-image-worker
  TAG: ${{ inputs.tag || 'qwen-image-edit-2511-q8-20260914-r1' }}

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Check runner storage
        shell: bash
        run: df -h /

      - name: Reclaim runner storage for model-bearing build
        shell: bash
        run: |
          set -euo pipefail
          sudo rm -rf \
            /usr/local/lib/android \
            /usr/share/dotnet \
            /opt/ghc \
            /usr/local/share/powershell \
            /usr/local/lib/node_modules \
            /opt/hostedtoolcache
          sudo docker system prune --all --force
          df -h /

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to public GHCR package
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PUSH_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Build and publish model-bearing image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: containers/qwen-image-edit-worker/Dockerfile
          target: runtime
          push: true
          provenance: false
          sbom: false
          tags: ${{ env.IMAGE }}:${{ env.TAG }}

      - name: Verify published image manifest
        shell: bash
        run: |
          set -euo pipefail
          docker buildx imagetools inspect "${IMAGE}:${TAG}"
          docker buildx imagetools inspect "${IMAGE}:${TAG}" --raw \
            | jq -e '.config.digest and (.layers | length > 0)' >/dev/null

      - name: Verify exact tag is anonymously pullable
        shell: bash
        run: |
          set -euo pipefail
          repository="${IMAGE#ghcr.io/}"
          manifest_url="https://ghcr.io/v2/${repository}/manifests/${TAG}"
          registry_token=$(curl --silent --show-error --fail --get \
            --data-urlencode 'service=ghcr.io' \
            --data-urlencode "scope=repository:${repository}:pull" \
            'https://ghcr.io/token' | jq -r '.token')
          test -n "${registry_token}" -a "${registry_token}" != null
          for attempt in 1 2 3 4 5; do
            status=$(curl --silent --show-error --output /dev/null --write-out '%{http_code}' \
              --header "Authorization: Bearer ${registry_token}" \
              --header 'Accept: application/vnd.oci.image.index.v1+json, application/vnd.oci.image.manifest.v1+json' \
              "${manifest_url}")
            echo "Anonymous GHCR manifest check ${attempt}/5: HTTP ${status}"
            if [ "${status}" = "200" ]; then
              exit 0
            fi
            sleep 10
          done
          echo "The public Qwen Image Edit tag is not anonymously pullable." >&2
          exit 1
